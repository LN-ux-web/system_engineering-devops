#!/usr/bin/env bash
# This script lists filenames in sorted order, showing only the part after the first dash

for file in $(ls | sort); do
  if [[ "$file" == *-* ]]; then
    echo "${file#*-}"
  fi
done

